            total_count = left_count + right_count + 1

            average = total_sum // total_count

            if average == node.val:
                ans += 1

            return total_sum, total_count

        dfs(root)
        return ans

