        def merge(a, b):

        tree = [None] * (4 * n)

        #         prefix, suffix, best]
        # node = [first_char, last_char, length,

        n = len(s)

    def longestRepeating(self, s, queryCharacters, queryIndices):
class Solution:

