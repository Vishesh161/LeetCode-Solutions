
            # Query [start, n-1]
            node = tree.query(
                1, 0, n - 1,
                start,
                n - 1
            )

            ans.append(node.cnt[x])

        return ans

