            prefix -= stones[i + 1]
            ans = max(ans, prefix - ans)

        return ans
        ans = prefix

        for i in range(len(stones) - 2, 0, -1):
        prefix = sum(stones)
    def stoneGameVIII(self, stones: List[int]) -> int:
class Solution:

